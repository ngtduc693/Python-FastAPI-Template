{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run FastAPI Server",
            "type": "shell",
            "command": "poetry run uvicorn app.main:app --reload",
            "group": "build",
            "problemMatcher": []
        },
        {
            "label": "Run Tests with Coverage",
            "type": "shell",
            "command": "poetry run pytest --cov=app",
            "group": "test",
            "problemMatcher": []
        }
    ]
}
